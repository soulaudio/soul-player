# SQLx compile-time database URL (for development)
# Copy this file to .env and the build system will use it
# Note: This is NOT needed for runtime - only for compilation
DATABASE_URL=sqlite:libraries/soul-storage/.tmp/dev.db
